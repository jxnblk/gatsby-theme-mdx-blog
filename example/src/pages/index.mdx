import Readme from '@jxnblk/gatsby-theme-mdx-blog/README.md'
import { Link } from 'gatsby'
import Layout from '../layout'

export default Layout

This is an example site built with `@jxnblk/gatsby-theme-mdx-blog`.

View the <Link to='/blog'>Blog</Link> to see it in action or view the <Link to='/unstyled'>Unstyled page</Link> to see how styles are isolated from the global scope.

<Readme />
